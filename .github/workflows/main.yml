name: testing_ci

on:
  push:
    branches:
      - master


jobs:
    say-hello-test:
     runs-on: ubuntu-24.04
     steps:
        - run: echo "this is my first github actions"
        
        
